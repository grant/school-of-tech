class Curriculum {
  constructor() {
    // quarterToClasses: A map from quarter id to classes offered that quarter (classId)
    this.quarterToClasses = {
      0: {
      }
    }
  }
}

export default Curriculum;